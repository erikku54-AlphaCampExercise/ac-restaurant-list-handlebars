<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/bootstrap.css">
    {{!-- <link rel="stylesheet" href="/stylesheets/fontawesome-all.css"> --}}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Restaurant List</title>
</head>
<body>
    <header>
    <!-- navigation -->
        <nav class="navbar navbar-light bg-light">
            <div class="container">
                <a class="navbar-brand" href="/">
                    <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png" width="30" height="30" class="d-inline-block align-top" alt="我的餐廳清單">
                    {{#if isAuthenticated}}{{user.name}}的{{/if}}餐廳清單
                </a>
                {{!-- <a class="btn btn-secondary" href="/restaurants/new">新增餐廳</a> --}}

                {{#if isAuthenticated}}
                    <a class="btn btn-secondary" href="/users/logout">登出</a>
                {{else}}
                    <a class="btn btn-secondary ms-auto me-2" href="/users/login">登入</a>
                    <a class="btn btn-secondary" href="/users/register">註冊</a>
                {{/if}}
            </div>
        </nav>

        <div class="banner"></div>
    </header>

    {{!-- use in partial template --}}

    {{{body}}}

    <script src="/javascripts/bootstrap.bundle.js"></script>
</body>
</html>